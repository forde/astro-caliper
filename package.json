{
  "name": "astro-caliper-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently 'pnpm --filter astro-caliper dev' 'pnpm --filter demo dev'",
    "copy-readme": "cp README.md packages/astro-caliper/README.md",
    "build": "pnpm --filter astro-caliper build",
    "release": "pnpm changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "concurrently": "^9.2.1"
  }
}